module.exports = {
  presets: [
    "@babel/preset-env",
    // Altera funcionalidades do JS que o navegador não entende
    "@babel/preset-react"
    // Transforma funcionalidades do React que o navegador não entende
  ],
}